// No verified customer testimonials exist yet — this stays an empty array
// rather than invented names/quotes/ratings (see brand content rule in
// README.md and the "Testimonials" section of the Phase 2 brief). The
// homepage testimonials section renders a tasteful "incoming" state until
// real entries are added here.
//
// Shape each future entry should follow:
// { id, quote, author, location, projectType }
export const testimonials = []
